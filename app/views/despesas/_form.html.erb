<%= form_for([caixa,despesa]) do |form| %>
  <% if despesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(despesa.errors.count, "error") %> prohibited this despesa from being saved:</h2>

      <ul>
      <% despesa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nome %>
    <%= form.text_area :nome, id: :despesa_nome, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :valor %>
    <%= form.text_field :valor, id: :despesa_valor, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Salvar Despesa", class: "btn btn-success" %>
  </div>
<% end %>
